# nginx-rtmp-recorder
This is a simple docker project that compiles a version of NGINX with the RTMP module and provides a config to allow it to be used as a recorder.

This project has (at the time of writing) not been maintained for 2 years and should not be used in production. 

Updates pending